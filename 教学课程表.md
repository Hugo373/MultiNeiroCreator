# 教学课程表（互动带学进度追踪）

> 用途：记录"老师带学"模式的课程规划与进度，防止对话记录丢失后课程断档。
> 规则：每讲完一节就把状态更新为 ✅ 并写上日期；每节课的收获与错题同步记进《学习笔记与常错点.md》。
> 状态标记：`[ ]` 未开始 / `[-]` 进行中 / `✅` 已完成
>
> 教学方式约定：先讲原理（用本项目真实代码当教材）→ 课堂提问（难度递增）→ 学生作答 → 讲评纠错 → 一起动手修对应问题 → 修完更新 unsolved.md/solved.md。
>
> **⚠️ 节奏调整（2026-08-12）**：改为「先修复 → 再功能 → 最后集中复盘教学」。除**第四课（C2/C3/C4）保留互动教学、边教边改**外，其余课程对应的问题由 AI 直接修复（solved.md 写详细当教材底稿），课程本身转为日后的「代码考古式」复盘讲解。

---

## 总体路线（对应 unsolved.md 分区）

| 顺序 | 模块 | 对应分区 | 状态 |
|---|---|---|---|
| 1 | C 区：后端架构还债 | C1~C11 | `[-]` 进行中 |
| 2 | G 区：性能与 RAG 优化 | G1~G4 | `[ ]` |
| 3 | E 区：多 Agent / 任务队列 | E2~E4 | `[ ]` |
| 4 | H 区：文档与简历指标 | H1~H3 | `[ ]` |

（A/B/D/F 区已在开课前修复完毕，相关技术点如有需要可作专题回顾。）

---

## C 区课程表（当前模块）

### 第一课：数据库层 —— SQLite 并发模型、索引、外键、连接生命周期
- 对应问题：C7（SQLite 裸奔）、C8（连接泄漏）、C9（建表分裂无迁移）
- 教材：`backend/core/database.py`、`backend/core/migrations.py`、`backend/repositories/chat_repo.py`
- 状态：✅ 结课（2026-08-12；C7/C8 互动教学修完，C9 按新节奏由 AI 直接修复，复盘时补讲迁移机制）
- 进度明细：
  - ✅ 理论（上）：SQLite 库级写锁 vs 行级锁、WAL 模式、busy_timeout、外键默认关闭的历史包袱
  - ✅ 课堂提问 3 道已作答 + 讲评（暴露基础盲区：表/索引/连接概念，已补课，错题记入笔记）
  - ✅ 复测 2 道 + 最左前缀验收题（复测 A 结论对理由错已纠；复测 B 落入预埋陷阱引出最左前缀；验收题全对，WAL 机制能用自己的话复述含 checkpoint）
  - ✅ 理论（下）：最左前缀原则、连接=电话类比重讲、EXPLAIN QUERY PLAN 诊断工具
  - ✅ 动手：修 C7（WAL + synchronous=NORMAL + 显式 timeout + foreign_keys + 两个索引；EXPLAIN 前后对比 SCAN→SEARCH；提交 7b0867f，solved.md 第 32 条）
  - ✅ 动手：修 C8（`db_connection()` 上下文管理器，11 处泄漏全清，事故重演实验验证回滚+锁释放；提交 ae2aac6，solved.md 第 33 条）
  - ✅ 动手：修 C9（DDL 收拢 `core/migrations.py` + user_version 顺序迁移 + 重建表补 FK 声明；真实库副本冒烟验证；提交 be46e89，solved.md 第 34 条；按新节奏由 AI 直接修复，待复盘）

### 第二课：分层架构 —— repository 模式、领域模型（转复盘模式）
- 对应问题：C5（models 层空置、Project 三处定义）、C6（service 混裸 SQL 与 DDL）、C11（30 个空占位文件）
- 教材：`backend/services/project_service.py`、`backend/repositories/`、`backend/models/`
- 状态：✅ 问题已由 AI 直接修复（2026-08-12，提交 c735fcf，solved.md 第 35 条），理论部分待复盘讲解
- 计划内容：
  - 理论：router / service / repository / model 各层职责边界；"同一概念多处定义"为什么是债；YAGNI 与空占位文件的骗局
  - ~~动手~~ → 由 AI 直接修 C6/C5/C11，日后按 solved.md 复盘讲解

### 第三课：asyncio 事件循环 —— 为什么一个同步调用拖死全站（转复盘模式）
- 对应问题：C1 剩余部分（/documents/reindex 串行 embedding 独占事件循环、service 层同步 DB 调用）
- 教材：`backend/services/assistant_service.py`、`backend/services/rag/`
- 状态：✅ 问题已由 AI 直接修复（2026-08-12，提交 f9d1e48，solved.md 第 36 条），理论部分待复盘讲解
- 计划内容：
  - 理论：事件循环单线程协作式调度、阻塞 vs 非阻塞、to_thread/iterate_in_threadpool 的原理与代价、什么时候该上任务队列（联动 E3）
  - 回顾：已修的两个 C1 案例（solved.md #18 流式阻塞、#25 工具同步执行）当反面教材
  - ~~动手~~ → 由 AI 直接修 C1 剩余部分，日后按 solved.md 复盘讲解

### 第四课：Agent 编排 —— 拆上帝函数、多轮工具循环、注册机制（**保留互动教学，边教边改**）
- 对应问题：C2（stream_chat 上帝函数）、C3（工具单轮单调用）、C4（工具扩展改 4 处）
- 教材：`backend/services/assistant_service.py`、`backend/agents/`
- 状态：`[ ]`
- 计划内容：
  - 理论：ReAct 循环（推理→行动→观察）、工具调用协议、注册器模式、编排器拆分（context_builder / tool_executor / chat_orchestrator）
  - 动手：修 C2/C3/C4，为 E2 多 Agent 工作流打地基

---

## 后续模块草案（开讲前细化）

- **G 区**：embedding 批处理与 task_type、RAG 召回质量（阈值/rerank/overlap）、token 截断策略、压测与基线（联动 H3 指标）
- **E 区**：多 Agent 工作流编排、任务队列（数据库任务表起步）、模型配置化
- **H 区**：技术文档写作（架构图/ADR/API 文档）、简历指标体系（P95/QPS/召回率/覆盖率）
