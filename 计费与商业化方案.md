# MultiNeiroCreator 平台计费与商业化方案（后置规划）

> 文档状态：产品与技术方案草案，当前不进入开发 Sprint  
> 决策日期：2026-08-20  
> 关联任务：`unsolved.md` 的 **I. 商业化与计费（独立后置分区）**  
> 启动前置条件：E3 worker、E2 多 Agent 工作流和平台主流程全部跑通并完成回归

---

## 1. 这份文档解决什么问题

MultiNeiroCreator 面向普通创作者。默认要求用户自行申请模型账号、寻找 Base URL、填写模型名和 API Key，使用门槛过高，也会让“打开即用”的产品体验断裂。

因此产品默认方案调整为：

> **平台统一接入模型供应商并保管平台 API Key；用户选择模型后直接使用。用户购买月度套餐获得包含额度，额度用完后从预付余额按量扣费。**

BYOK（用户自带 API Key）不作为默认路径，只保留为远期高级功能的可能选项。

本文只冻结方向、边界、数据口径和未来任务，不代表现在开始实现支付系统。

---

## 2. 已确定的产品决策

### 2.1 默认体验：平台托管、点击即用

普通用户只需要：

```text
注册/登录 → 选择平台提供的模型 → 发送消息 → 查看额度消耗
```

用户不需要理解或填写：

- API Key；
- Base URL；
- 上游厂商的真实模型名称；
- OpenAI-compatible 等接口协议；
- 各厂商的余额与限流规则。

### 2.2 收费方式：月包 + 预付超额

```text
月度套餐额度
    ↓ 用完
预付余额按量扣除
    ↓ 不足
停止新调用并提示充值/升级
```

早期不做“先使用、月底再追缴”的信用后付费，不允许余额被扣成负数。原因是模型供应商已经向平台计费，而恶意用户、盗号用户或支付失败用户未必会向平台付款。

### 2.3 当前阶段只使用智谱模型

在 worker、工作流和平台主流程完成前：

- 后端继续使用平台配置的智谱 API Key；
- 聊天模型保持 `glm-4-flash` 单模型主链；
- 先保证普通聊天、SSE、工具调用、RAG 和后续 Agent 节点稳定工作；
- 不为了尚未启用的付费功能提前开发多供应商路由、套餐、钱包或支付回调。

当前仓库已经具备 `ZhipuAI` 调用链，开发环境的 `API_KEY` 已配置；仍需通过真实端到端回归确认聊天和工具链可正常使用。未来商业化改造时应把通用名称 `API_KEY` 收敛为明确的供应商凭据名称，例如 `ZHIPU_API_KEY`。

---

## 3. 为什么不能按“消息条数”计费

一条用户消息不等于一次上游模型调用。现有 ReAct 编排可能发生：

```text
第一次模型调用
→ 模型请求搜索工具
→ 工具返回结果
→ 第二次模型调用
→ 模型请求计算工具
→ 工具返回结果
→ 第三次模型调用并给出答案
```

此外，短问候与携带大量历史、RAG 资料的长请求成本差异很大。因此不能使用“每条消息固定扣一次”作为真实账务口径。

未来必须按每一次上游调用记录至少以下数据：

- 模型和供应商；
- 输入 token；
- 输出 token；
- 缓存 token、推理 token 等供应商特有计费项；
- 供应商请求 ID；
- 上游实际成本；
- 向用户结算的积分/金额；
- 成功、失败、取消状态；
- 对应的用户请求、项目和工作流节点。

计量单位是“每一次上游调用”，不是“每一条前端消息”。多 Agent 工作流中的每个 Agent 节点也必须遵守同一口径。

---

## 4. 用户看到积分，后台保留真实成本

普通用户可以看到更易理解的 AI 积分：

```text
本月套餐剩余：820 AI 积分
预付余额：30.00 元
本次运行消耗：6.4 AI 积分
```

后台必须同时保存两套值：

1. `provider_cost`：平台向模型供应商承担的真实成本；
2. `billed_credits` / `billed_amount`：平台向用户扣除的额度。

两套值不能混为一个字段，否则无法计算毛利、解释账单或调整价格。

金额与积分计算必须使用定点数/`Decimal`，不能使用二进制浮点数。模型单价需要版本化并记录生效时间；历史账单必须引用调用发生时的价格快照，不能因管理员后来改价而变化。

---

## 5. 未来模型层的边界

用户的聊天请求只发送平台模型 ID：

```json
{
  "message": "帮我设计一个剧情",
  "model_id": "neyria-flash",
  "project_id": 3
}
```

后端模型目录负责解析：

- 使用哪个供应商；
- 调用哪个上游模型；
- 使用哪一个平台凭据；
- 是否支持工具调用、图片或结构化输出；
- 当前是否启用；
- 哪些套餐可以使用；
- 当前价格版本和额度倍率。

前端不得提交任意 `base_url`、供应商 API Key 或未经平台登记的模型名，避免绕过计费、凭据泄露和 SSRF 风险。

后续建议用统一适配器隔离供应商差异：

```text
services/models/
├── base.py
├── registry.py
├── zhipu.py
├── openai_compatible.py
└── anthropic.py        # 真正需要时再实现
```

模型适配器除了输出流式内容，还必须返回标准化 usage，供计量系统消费。

---

## 6. 核心数据设计草案

表名和字段可在实施 ADR 中调整，但下面的职责边界应保留。

### 6.1 `model_catalog`：平台模型目录

```text
id
provider
upstream_model
display_name
description
enabled
supports_tools
supports_vision
minimum_plan
current_price_version_id
created_at
updated_at
```

用户只看到平台允许使用且与套餐匹配的模型。

### 6.2 `model_price_versions`：模型价格版本

```text
id
model_id
input_unit_price
output_unit_price
cache_unit_price
reasoning_unit_price
credit_multiplier
effective_from
effective_to
```

每条使用记录引用确定的价格版本，保证历史可追溯。

### 6.3 `subscriptions`：订阅周期

```text
id
user_id
plan
status
period_start
period_end
included_credits
auto_renew
payment_subscription_id
created_at
updated_at
```

状态至少区分：`active`、`expired`、`cancelled`、`past_due`。

### 6.4 `usage_events`：不可变用量事件

```text
id
request_id
user_id
project_id
workflow_run_id
workflow_step_id
model_id
provider
provider_request_id
prompt_tokens
completion_tokens
cached_tokens
reasoning_tokens
provider_cost
billed_credits
price_version_id
status
created_at
```

每次真实上游调用写一条；失败、取消也要记录状态，不能只记录成功请求。

### 6.5 `credit_ledger`：余额流水

```text
id
user_id
amount
balance_after
type
reference_id
description
created_at
```

流水类型至少包括：

- `subscription_grant`：月包发放；
- `usage_reserve`：调用前预占；
- `usage_settlement`：按真实用量结算；
- `usage_release`：退回多预占额度；
- `top_up`：充值；
- `refund`：退款；
- `expiration`：套餐额度到期；
- `manual_adjustment`：管理员调整。

不能只在用户表里保存一个可直接覆盖的余额数字，否则无法解释余额变化或完成对账。

### 6.6 `payment_orders`：支付订单

```text
id
user_id
provider
provider_order_id
idempotency_key
order_type
amount
currency
status
created_at
paid_at
```

支付回调必须验签并做幂等处理；同一回调重复到达不能重复发放额度。

---

## 7. 调用前预占、调用后结算

仅在模型生成完成后扣费会产生并发透支：同一用户可以同时发起多个请求，每个请求都在开始时看到“余额足够”，最后一起把余额扣成负数。

推荐流程：

```text
收到请求
→ 校验模型、套餐、余额和消费上限
→ 在事务中预占额度
→ 调用模型并逐次记录上游 usage
→ 按真实 usage 计算费用
→ 在事务中结算
→ 退回多预占额度或补扣差额
```

预占、结算、释放必须可重试且幂等。需要数据库事务或原子条件更新，禁止“先查余额、再普通 UPDATE”这种竞态写法。

如果实际费用可能超过预占值，应限制单次最大输入、最大输出、工具轮数和工作流预算；早期仍以“不产生负余额”为硬约束。

---

## 8. 流式、工具和工作流的特殊情况

### 8.1 SSE 中途断开

用户关闭页面不代表上游没有产生成本。后端应：

1. 尝试取消上游请求；
2. 停止不必要的后续 Agent/工具步骤；
3. 保存已经产生的 usage；
4. 按实际成本结算；
5. 把运行标记为 `cancelled`，而不是当作免费调用。

### 8.2 工具调用失败

搜索或计算失败不代表模型调用免费。每一轮模型已经产生的 usage 仍应记录，工具自己的第三方成本以后也可以接入同一计量事件体系。

### 8.3 worker 重试

worker 超时后重试可能造成上游调用重复。任务、模型调用和账务事件都需要稳定的幂等键。必须区分“本地没收到结果”和“供应商没有执行”，不能无条件重试并重复扣费。

### 8.4 工作流预算

多 Agent 工作流应支持：

- 单次运行预算；
- 单节点预算；
- 最大重试次数；
- 用户确认后才继续的昂贵节点；
- 达到预算后暂停，而不是无限运行。

这也是先完成 E3/E2 再实现计费的原因：只有工作流的状态、取消、恢复和重试语义确定后，账务规则才能正确落地。

---

## 9. 风控与成本保护

平台托管 API Key 后，用户消耗的是平台真实资金。现有按请求次数限流仍需保留，但不足以代替消费控制。

商业化前至少补齐：

- 用户级并发上限；
- 单次输入/输出 token 上限；
- 每次工具轮数和调用数上限；
- 每个工作流的最大预算；
- 每日、每月消费上限；
- 新账号低额度与邮箱验证；
- 异常消费告警；
- 上游余额和限额告警；
- 单模型全局预算；
- 模型熔断和管理员紧急关闭开关；
- API Key、支付信息和账务日志脱敏；
- 退款、人工调整和审计记录。

正式收费前还要确认模型供应商协议是否允许当前产品形态的商业使用或转售，并补齐支付、退款、隐私和服务协议。

---

## 10. 分阶段实施计划

### 阶段 0：当前——智谱单模型稳定基线

目标：先让智能体和平台核心功能稳定工作。

- 使用平台智谱 API Key 和 `glm-4-flash`；
- 回归普通流式聊天；
- 回归单轮、多轮和多工具调用；
- 回归 RAG 对话；
- 后续 worker 和多 Agent 节点统一复用该模型链；
- 真实外部 API 只用于必要的人工验收，自动化测试继续使用 FakeLLM/mock，避免消耗额度。

### 阶段 1：E3 worker + E2 多 Agent + 主流程跑通

目标：完成项目的核心作品价值。

- 数据库任务表和 worker；
- 工作流状态持久化、取消、恢复、重试；
- 规划 → 专项创作 → 审核 → 用户确认 → 保存资产；
- 注册、项目、聊天、RAG、工作流、资产全链路回归。

该阶段不接真实支付。

### 阶段 2：模型目录与精确计量

目标：先证明“每一次上游调用到底花了多少”。

- 平台模型目录和统一模型适配器；
- 动态模型菜单；
- 每次上游调用的 usage 采集；
- 价格版本和真实成本计算；
- 普通聊天、ReAct、worker、工作流统一计量；
- 管理员可查看成本，但暂不向用户真实扣款。

### 阶段 3：额度账本与模拟套餐

目标：在不接支付渠道的情况下验证账务正确性。

- 套餐周期；
- 管理员发放测试额度；
- 不可变余额流水；
- 预占、结算、释放；
- 余额不足拒绝；
- 用量与账单页面；
- 并发、失败、取消、重试和幂等测试。

### 阶段 4：真实支付与上线风控

目标：在计量和账本稳定后接入资金链路。

- 月包购买与续费；
- 预付余额充值；
- 超额按量扣除；
- 支付回调验签和幂等；
- 退款与对账；
- 消费提醒和硬上限；
- 供应商协议、用户协议和隐私说明；
- 小范围灰度后再正式开放。

### 阶段 5：可选 BYOK

仅在真实用户明确需要时评估：

- 高级用户自行配置 API Key；
- BYOK 调用不消耗平台模型额度，或只收取平台服务费；
- 凭据必须加密保存且不可回显；
- 该能力不影响普通用户默认的点击即用体验。

---

## 11. 开始商业化开发的硬门槛

以下条件未满足时，`unsolved.md` 的 I 区保持 `[~]` 主动延期：

- [ ] E3 worker 已完成，任务可恢复、可取消、可幂等重试；
- [ ] E2 多 Agent 工作流已完成；
- [ ] 注册/登录 → 项目 → 对话 → RAG → 工作流 → 用户确认 → 保存资产全链路跑通；
- [ ] 智谱模型在聊天、工具和工作流节点中稳定可用；
- [ ] SSE 断开、模型失败、工具失败、worker 重试的语义已经确定；
- [ ] 核心流程已有自动化测试和至少一轮人工回归；
- [ ] 已确认首个支付渠道和模型供应商商业使用条款。

---

## 12. 未来验收标准

商业化功能只有同时满足以下条件才算完成：

- [ ] 用户无需 API Key 即可选择平台模型并使用；
- [ ] 每次上游模型调用都有唯一、可追溯的 usage 事件；
- [ ] 一条消息触发多轮工具或多个 Agent 时不会漏记调用；
- [ ] 历史费用引用调用时的价格版本，修改现价不影响旧账；
- [ ] 余额预占与结算在并发下不会透支或重复扣款；
- [ ] SSE 取消、上游失败和 worker 重试都有明确计费结果；
- [ ] 月包额度、预付余额和消费流水可向用户解释；
- [ ] 余额不足时在调用上游前拒绝，不产生平台坏账；
- [ ] 支付回调重复发送不会重复发放额度；
- [ ] API Key、支付信息和内部成本不会泄露到前端或日志；
- [ ] 管理员可以关闭异常模型、用户或全局付费调用；
- [ ] 完成支付沙箱、并发扣费、退款、对账和故障恢复测试。

---

## 13. 实施前仍需确认的产品问题

这些问题现在不阻塞 E3/E2，进入商业化阶段前再冻结：

1. 免费版是否存在，以及免费额度按日还是按月发放；
2. 套餐包含额度是否到期清零、是否允许结转；
3. 用户界面展示积分、人民币，还是二者同时展示；
4. 不同模型采用独立额度还是统一积分倍率；
5. 套餐额度和充值余额的扣除优先级；
6. 用户是否可以关闭超额扣费并设置单次/每日预算；
7. 首个支付渠道、自动续费与退款规则；
8. 首批开放哪些模型，以及供应商协议是否允许该商业模式；
9. BYOK 是否有真实用户需求，是否只向高级套餐开放。

在上述决策确定前，不应把临时数字写死进业务代码。
