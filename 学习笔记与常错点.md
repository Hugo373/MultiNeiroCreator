# 学习笔记与常错点

> 用途：沉淀每节课学到的知识点 + 答错/理解偏差的地方，供复习和面试前速览。
> 规则：每节课结束后由老师（AI）整理追加，格式固定为"知识点速记"和"错题本"两部分；
> 错题本记录：题目 → 我当时的答案 → 正确答案 → 为什么错。

---

## 知识点速记

### C 区第一课（上）：SQLite 并发模型（2026-08-12）

- **SQLite 唯一的硬限制**：同一时刻只允许一个写事务（库级写锁），而 MySQL/Postgres 是行级锁。读性能单机很强，"是个文件"不等于玩具。
- **journal 模式**：默认 rollback journal 下，写事务会阻塞读；改 `PRAGMA journal_mode=WAL` 后写走 `.wal` 追加文件，**读写互不阻塞**，只剩写与写互斥。
- **busy_timeout**：拿不到锁时默认**等 0 毫秒直接抛 `database is locked`**；`PRAGMA busy_timeout=5000` 变成最多等 5 秒。大多数 locked 报错是"没给它等的机会"，不是并发真顶不住。
- **外键默认关闭**：建表写了 `FOREIGN KEY` 不等于生效，必须每个连接 `PRAGMA foreign_keys=ON`，否则约束是注释。这是 SQLite 为兼容老代码留的历史包袱。
- **本项目现状**（C7）：`get_connection()` 就一句 `sqlite3.connect(DB_FILE)`，以上开关全没动。

### C 区第一课（地基补课）：表/索引/连接的直观图像（2026-08-12）

- **数据都在一个文件里**：SQLite 数据库 = 一个 .db 文件 ≈ Excel 工作簿；users/messages/projects 是里面的三张工作表。
- **messages 表**：存所有人的聊天记录，前端发一句 = 后端 INSERT 一行；不是"对话窗口"这个 UI 概念。
- **索引的直观模型**：10 万页日记本找 50 页 → 无目录只能逐页翻（全表扫描）；索引就是按列排序的目录，查找像查字典。代价：每次写入要同步更新目录。
- **连接的直观模型**：连接 ≈ open() 打开文件的句柄，用完必关；异常会跳过后续语句，所以裸写 close() 不可靠，必须 with/try-finally。
- **泄漏的可怕之处**：错误不留在原地——一个请求泄漏的连接攜带写锁，会让后续所有写请求连环失败。

- **最左前缀原则**（复测 B 补课）：复合索引 (A,B) 是先按 A 再按 B 排的目录，不从 A 进门就用不了；索引只影响速度，不影响结果正确性。

### C 区第一课（下）+ 动手修 C7（2026-08-12，提交 7b0867f）

- **EXPLAIN QUERY PLAN**：让 SQLite 自己交代"打算怎么执行查询"：`SCAN` = 全表扫描（坏），`SEARCH ... USING INDEX` = 索引定位（好）。修性能问题标准姿势：改前拍现场 → 改 → 同一工具复验。
- **当堂纠错（老师也会错）**：busy_timeout 的 C 库默认是 0，但 **Python sqlite3 包装层默认传 timeout=5.0**——"默认值"要看自己入口那一层，不能背结论；显式声明优于依赖默认。
- **PRAGMA 的作用域差异**：`journal_mode=WAL` 持久化在库文件上（设一次永久生效）；`foreign_keys`、`busy_timeout` 是每连接各自设——所以连接配置必须收拢到唯一入口 get_connection()。
- **synchronous=NORMAL**：WAL 推荐搭档，fsync 从每次提交降为 checkpoint 时；掉电最多丢最近几次提交，不损坏库。
- **索引跟着真实查询走**：先 grep 全部 WHERE/ORDER BY 盘点，再决定建什么；UNIQUE 列（users.username）自带唯一索引不重复建。
- **意外收获**：勘查发现 project_service 绕过 get_connection() 自己 sqlite3.connect——同库两套开门方式，C6 分层混乱的实物证据（第二课教材）。

### 动手修 C8（2026-08-12，提交 ae2aac6）

- **sqlite3 的 with 陷阱**：`with conn:` 管的是事务（正常 commit / 异常 rollback），**不会 close**！和文件对象的 with（自动关闭）语义不同，不能望文生义。完整保护 = 外层 try/finally close + 内层 with 事务。
- **try/finally**："不管上面成没成，finally 块必执行"——with 的手动版，with 底层就是它。
- **@contextmanager + yield**：把"拨号→借出连接→必挂电话"打包成可复用配方；yield = 把连接递给使用者，用完（或炸了）回来继续走 finally。好抽象的标志：**把容易忘的事变成不可能忘的事**。
- **破坏性验证**：修复类问题要故意重演事故（事务中途 raise）验证兑承诺：异常外抛 ✓ 半截数据回滚 ✓ 锁释放后续可写 ✓。只验正常路径等于没验。
- 实测泄漏点 11 处（评估说 9 处）：问题清单会过期，动手前重新盘点。

### C 区第一课（下）：迁移机制（2026-08-12，提交 be46e89）

- **迁移的本质**：给数据库结构一个单调递增的版本号；启动时只执行当前版本之后的迁移，历史迁移不修改。
- **本项目实现**：`core/migrations.py` 使用 SQLite `PRAGMA user_version`，把 DDL 集中管理；迁移 002 通过重建表补充外键，因为 SQLite 不支持直接 `ALTER TABLE ... ADD FOREIGN KEY`。
- **三种起点都要考虑**：全新库、已有旧库、迁移中途失败后重试。`IF NOT EXISTS`、清理临时表和 `foreign_key_check` 共同保证迁移可重跑。
- **外键的两个开关**：`PRAGMA foreign_keys=ON` 只是让约束检查生效，表定义里还必须真的写 `REFERENCES`；只开开关而不声明外键等于警报器没有接到门上。
- **验证方式**：检查 `user_version`、`foreign_key_list`、`foreign_key_check`，并在真实数据库副本上验证升级前后数据数量和幂等性。


---

## 错题本

### C 区第一课课堂提问（作答后补记）

- **第 1 题**（并发）：两个请求同时写不同的表（messages / projects），当前配置下会怎样？
  - 我的答案："应该会互相影响"（结论对），但把 messages 理解成了"对话窗口"、projects 理解成了"写在本地的东西"——没建立"表 = 数据库文件里的工作表"的图像。
  - 正确答案：会互相影响。**SQLite 的写锁是整个数据库文件级的**（不是表级/行级），两张表住在同一个 .db 文件里，A 写 messages 时 B 写 projects 也得等；叠加 busy_timeout=0 默认值，B 不等直接抛 `database is locked`。
  - 错因：基础概念缺口——表/库/文件的关系。已补课：整个后端数据存在一个 SQLite 文件里，users/messages/projects 是其中三张表；messages 存的是聊天记录本身（前端窗口发一句 = 后端 INSERT 一行）。
- **第 2 题**（索引）：`WHERE user_id=? AND project_id=? ORDER BY id` 无索引时 10 万行怎么找 50 行？索引怎么建？
  - 我的答案：不知道，看不懂题目——不知道索引是什么。
  - 正确答案：无索引 = **全表扫描**（10 万行逐行看条件）；建复合索引 `CREATE INDEX idx_messages_user_project ON messages(user_id, project_id)` 后变成查字典式定位，几十次比较定位 50 行。
  - 补课要点：索引 = 按指定列排好序的"目录"，本质权衡是**用写入变慢一点换查询快很多**，只建在高频 WHERE 列上，不是越多越好。
- **第 3 题**（连接泄漏）：`conn.execute` 抛异常后 `conn.close()` 不执行，连锁反应是什么？
  - 我的答案：看不懂——不知道"连接"是什么。
  - 正确答案：异常直接跳出函数，close 不执行 → 没关的连接可能攜带着整库写锁成为"僵尸" → 后续所有写请求 `database is locked` → **一个请求的失败传染全站**。修法：`with` 上下文管理器（退出块时无论正常/异常都保证清理）。
  - 补课要点：连接 = 打开数据库文件的一次会话，规矩同文件句柄：用完必须关，且要用 with/try-finally 保证异常路径也关。
  - **重讲版（电话类比，第二遍才懂）**：连接 = 和数据库之间没挂的一通电话，数据库同时只接一个"要写东西"的电话（写锁）；异常 = 通话中途炸了，后面的"挂电话"不执行，电话被扔桌上占线，后续所有人打不进来；`with` = 不管炸没炸，出门时保证帮你挂电话。

### C 区第一课复测（2026-08-12）

- **复测 A**（WAL 后读写是否阻塞）：
  - 我的答案："不阻塞"（结论对），但理由是"各行不互相阻碍"——行级锁世界观，与第 1 题刚学的结论矛盾。
  - 正确理由：是 **WAL 机制**——写者追加写 .wal 日志文件，读者读主文件快照，读写分道。口诀：**WAL 之前写者挡路；WAL 之后读写分道；但写与写永远排队**。
  - 错因：把原因归到行/表粒度——SQLite 根本没有行级/表级锁，锁粒度永远是整库；"不阻塞"是 WAL 把读写路径分开了，不是锁变细了。
- **复测 B**（索引 (user_id, project_id)，查询只有 `WHERE project_id=7`）：
  - 我的答案："能帮上忙，查什么就返回什么"——混淆了**结果正确性**和**查询速度**（没索引结果也永远是对的，索引只影响快慢）。
  - 正确答案：几乎帮不上。目录先按 user_id 排序，project_id=7 散落在每个用户名下，无法跳跃定位。**最左前缀原则**：索引 (A,B) 能加速 `WHERE A=?` 和 `WHERE A=? AND B=?`，加速不了只有 `WHERE B=?`。类比：电话簿按"姓、名"排，查"张伟"秒到，找所有名叫"伟"的只能整本翻。

---

### C 区第四课（C2）课堂提问（2026-08-12）

三题开题版全部不会（设计类题目零基础，属正常），已补课后给出答案：

- **Q1**（clean_history 属于谁）：看**谁消费它**——clean_history 最终塞进 done 事件发给**前端**，所以属编排器（它管与前端的 SSE 约定）；messages 喂**模型**，属 context_builder。口诀：**给每个变量找消费者，消费者就是它的主人**。
- **Q2**（tool_executor 签名）：函数签名 = 合同。工具成功/被拒/抛异常三种结局，编排器做的事完全一样（把结果文本塞进 tool 消息继续对话），所以对外统一成 `ToolOutcome(result: str, ok: bool)`，**永不向外抛异常**；差异只体现在日志里。
- **Q3**（生成器边吐边交）：两解——A 登记本（传入可变 state 对象，子函数边 yield 边往里写汇总）；B 总结包（最后 yield 一个特殊类型，调用方认类型）。async 生成器没有 `yield from`，透传只能手写 `async for x in sub(): yield x`。
- 错因（共性）：缺三个地基概念——“职责=变化的理由/消费者”、“签名=合同”、“生成器=传送带”。已用餐馆/快递/传送带类比补课。

### C 区第四课（C2）复测（2026-08-12）：三道全对 ✅

- **复测 A**（截断历史归谁）：答 context_builder ✓——截断改变的是“喂给模型的东西”，消费者是模型，主人就是备菜的。
- **复测 B**（异常抛还是包）：选 (b) ✓，理由自己延伸到了日志价值。老师补精度：**主因是“调用方不分叉”**（编排器不管成败都只做同一件事：把结果转交模型），日志留痕是副产品。
- **复测 C**（生成器执行顺序）：收到×2 → 收工 ✓。传送带图像已建立。

结论：三个地基概念验收通过，可以动手拆 C2。

---

## 面试速览区（积累到一定量后按主题重组）

（暂空——每完成一课，把"能对面试官讲 30 秒的版本"提炼到这里）
